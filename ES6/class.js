class arrayOfCities{
    constructor(length, breadth){
        this.length = length
        this.breadth = breadth
    }

    AreaOfRectangle =function(){
        return this.length * this. breadth
    }

    AreaOfSquare = () => this.length * this.breadth
    AreaOfCircle =
}